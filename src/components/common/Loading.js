import React from 'react';

const Loading = () => {
    /* Helper component to indicate
     * request is running */
    return (
        <div className="loading">
            <p>Loading...</p>
        </div>
    )
};

export default Loading;
